<view
  class="container {{position}} {{show ? 'open' : 'close'}}"
>
  <ice-alert
    show-icon="{{showIcon}}"
    icon="{{icon}}"
    type="{{type}}"
    backgroundColor="{{backgroundColor}}"
    color="{{color}}"
    message="{{message}}"
  ></ice-alert>
</view>
