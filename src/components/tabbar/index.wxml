<view
  bind:change="change"
  class="i-tabbar__container {{shadow ? 'shadow' :''}} {{blur ? 'blur' : ''}} {{fixed ? 'fixed' : ''}}"
  style="background: {{backgroundColor}};"
>
  <slot></slot>
</view>
