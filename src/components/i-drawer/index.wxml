<view
  class="i-drawer__container {{placement}} {{open ? 'i-drawer__open' : 'i-drawer__close'}}"
>
  <!-- 蒙层 -->
  <view class="i-drawer__mask" bindtap="close"></view>
  <!-- 抽屉内容 -->
  <view class="i-drawer__content">
    <slot></slot>
  </view>
</view>
